import React from "react";
import { Typography } from "@mui/material";

export default function About() {
    return(
    <Typography>
    <h1 className="container">Add some about here</h1>
    </Typography>
    );
  }