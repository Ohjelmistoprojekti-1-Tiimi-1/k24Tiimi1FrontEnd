import React from "react";
import { Typography } from "@mui/material";

export default function Reservations() {
    return(
    <Typography>
    <h1 className="container">Add some reservations here</h1>
    </Typography>
    );
  }