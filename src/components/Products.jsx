import React from "react";
import { Typography } from "@mui/material";

export default function Products() {
    return(
    <Typography>
    <h1 className="container">Add some products here</h1>
    </Typography>
    );
  }